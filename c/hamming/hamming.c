#include "hamming.h"

int compute(const char *lhs, const char *rhs) {
  int counter = 0;
  int lhs_len = 0;
  int rhs_len = 0;

  while ()

    for (int i = 0; lhs[i] != '\0' && rhs[i] != '\0'; i++) {
      if (lhs[i] != rhs[i]) {
        counter++;
      }
    }

  return counter;
}
